---
import { Image } from "astro:assets";
import heroCapa from "../../assets/img/hero-capa.webp";
import HeroBackground from "./HeroBackground.astro";
---

<section
  class="relative py-16 xl:py-32 min-h-screen overflow-hidden bg-surface"
>
  <HeroBackground />

  <div
    class="max-w-7xl mx-auto px-6 grid lg:grid-cols-2 gap-16 items-center relative z-10"
  >
    <!-- IMAGEM (fica em cima no mobile) -->
    <div
      class="reveal visible hover-trigger relative group transition-all duration-700 ease-[cubic-bezier(0.23,1,0.32,1)] order-1 lg:order-2"
    >
      <!-- Glow -->
      <div
        class="absolute -inset-1 bg-linear-to-r from-primary to-indigo-700 rounded-2xl blur-md opacity-0 xl:opacity-10 group-hover:opacity-40 transition-all duration-700 ease-in-out"
      >
      </div>

      <div
        class="relative rounded-2xl overflow-hidden border border-white/10 shadow-2xl transition-all duration-500 mx-auto max-w-md lg:max-w-full"
      >
        <Image
          src={heroCapa}
          alt="JR Digital Studio"
          class="relative w-full h-auto transition-transform duration-700 ease-out"
          loading="eager"
        />
      </div>
    </div>

    <!-- TEXTO (fica embaixo no mobile) -->
    <div class="space-y-8 text-center lg:text-left order-2 lg:order-1">
      <div class="reveal">
        <span
          class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-primary/10 border border-primary/20 text-primary text-sm font-bold uppercase tracking-wider"
        >
          <i class="fa-solid fa-code"></i> Desenvolvedor Web • Joseph
        </span>
      </div>

      <h1
        class="reveal text-4xl sm:text-5xl lg:text-7xl font-black leading-[1.1] tracking-tight bg-linear-to-r from-white to-primary/60 bg-clip-text text-transparent"
        style="transition-delay: 0.2s;"
      >
        Sites profissionais para negócios modernos
      </h1>

      <p
        class="reveal text-base sm:text-lg text-text-muted max-w-xl mx-auto lg:mx-0"
        style="transition-delay: 0.4s;"
      >
        Desenvolvo sites rápidos, responsivos e com visual profissional para
        quem quer presença digital sem complicação.
      </p>

      <div
        class="reveal flex flex-wrap gap-4 justify-center lg:justify-start"
        style="transition-delay: 0.6s;"
      >
        <a
          href="https://wa.me/5591988649565?text=Olá%20Joseph!%20Gostaria%20de%20solicitar%20um%20orçamento%20para%20um%20projeto."
          target="_blank"
          class="bg-primary w-full md:w-fit hover:bg-primary-hover text-white px-8 py-4 rounded-xl font-bold gap-2 transition-all hover:-translate-y-1 shadow-xl shadow-primary-glow inline-flex items-center justify-center"
        >
          <i class="fa-brands fa-whatsapp text-xl"></i>
          Solicitar orçamento
        </a>

        <a
          href="#portfolio"
          class="bg-white/5 w-full md:w-fit hover:bg-white/10 border border-white/10 text-white px-8 py-4 rounded-xl font-bold transition-all hover:border-white/20"
        >
          Ver portfólio
        </a>
      </div>
    </div>
  </div>
</section>
